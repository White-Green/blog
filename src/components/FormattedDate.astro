---
interface Props {
	date: string;
}

const { date } = Astro.props;
const date_object = new Date(Date.parse(date));
---

<time datetime={date_object.toISOString()}>
	{
        date_object.toLocaleDateString('ja-jp', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
